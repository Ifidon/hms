extends layout

block content
	div.container
		div.row
			section.col-10.mx-auto.mb-3
				div.card
					div.card-header
						h5.text-center User Administration
					div.card-body
						a(href='/users/new') Register New User
						a.float-right(href='/users') View, Edit, and Delete Users

			table.table.table-striped.table-bodered.col-10.m-auto
				thead.thead-dark
					tr
						th Name
						th Email
						th Username
						th Role
						th Date Created
						th Action
						th Action
				tbody
				each user in users
					tr
						td= user.firstname + ' ' + user.lastname
						td= user.email
						td= user.username
						td= user.role
						td
						td
							a.btn.btn-secondary(href='/users/#{user.username}/edit') Edit Details
						td
						form(method='post' action='/users/#{user.username}/delete')
							button.btn.btn-danger(type='submit') Delete User

			script.
				var use ='#{user.username}'.toString()
			script(src='/javascripts/login.js')